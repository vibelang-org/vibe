// Program with conditional logic

model m = { name: "gpt-4", apiKey: "test", url: "http://test" }
let isPremium: boolean = true
let response = ""

if isPremium {
  let greeting = do "Generate a premium greeting" m default
  response = greeting
} else {
  response = "Standard greeting"
}

response
